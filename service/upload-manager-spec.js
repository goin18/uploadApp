describe('uploadManager', function() {

  beforeEach(module('restApp'));

  it('should ...', inject(function(uploadManager) {

	//expect(uploadManager.doSomething()).toEqual('something');

  }));

});